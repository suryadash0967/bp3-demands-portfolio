# ── Force binary wheels — avoids C++ build requirements on Windows ──────────
--prefer-binary

# ── Core ─────────────────────────────────────────────────────────────────────
fastapi>=0.111.0,<0.113.0
uvicorn[standard]>=0.30.0,<0.32.0
python-dotenv>=1.0.0
pydantic>=2.7.0,<2.9.0
pydantic-settings>=2.3.0,<2.5.0

# ── Excel parsing ─────────────────────────────────────────────────────────────
openpyxl>=3.1.0
pandas==2.3.1
numpy==2.2.6

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0,<3.2.0
# PyTorch CPU — install separately first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0,<0.6.0

# ── Forecasting (Python 3.13 compatible — no C++ required) ───────────────────
statsmodels>=0.14.0
# Prophet is NOT included — no Python 3.13 wheels available yet.
# To use Prophet, install Python 3.12 and reinstall.

# ── LLM (OpenAI-compatible) ────────────────────────────────────────────────────
openai>=1.35.0
httpx>=0.27.0

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── Logging ───────────────────────────────────────────────────────────────────
loguru>=0.7.0
